-- Initial database setup script
-- This runs when PostgreSQL container starts for the first time

-- Create database if not exists (though this is handled by POSTGRES_DB env var)
-- CREATE DATABASE IF NOT EXISTS house_design;

-- Optional: Create additional users or permissions here
-- CREATE USER app_user WITH ENCRYPTED PASSWORD 'app_password';
-- GRANT ALL PRIVILEGES ON DATABASE house_design TO app_user;
